import { ForbiddenNameValidatorReactiveFormDirective } from './forbidden-name-validator-reactive-form.directive';

describe('ForbiddenNameValidatorReactiveFormDirective', () => {
  it('should create an instance', () => {
    const directive = new ForbiddenNameValidatorReactiveFormDirective();
    expect(directive).toBeTruthy();
  });
});
