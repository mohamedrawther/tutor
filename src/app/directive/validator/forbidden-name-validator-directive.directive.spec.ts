import { ForbiddenNameValidatorDirectiveDirective } from './forbidden-name-validator-directive.directive';

describe('ForbiddenNameValidatorDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ForbiddenNameValidatorDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
